apiVersion: constraints.gatekeeper.sh/v1beta1
kind: EWExistingResources
metadata:
  name: service-must-have-no-pods-default-selector
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Service"]
  parameters:
    operations: ["DELETE"]
    existingResources:
      kind: Pod
