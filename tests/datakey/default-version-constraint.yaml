apiVersion: constraints.gatekeeper.sh/v1beta1
kind: EWDataKeySafetyCheck
metadata:
  name: configmap-key-still-referenced-defaults
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["ConfigMap"]
  parameters: {}
