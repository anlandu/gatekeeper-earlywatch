apiVersion: admission.k8s.io/v1
kind: AdmissionReview
request:
  operation: CREATE
  kind:
    group: ""
    version: v1
    kind: Service
  name: web
  namespace: default
  object:
    apiVersion: v1
    kind: Service
    metadata:
      name: web
      namespace: default
    spec:
      selector:
        app: web
