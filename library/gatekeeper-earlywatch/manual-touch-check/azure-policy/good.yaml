apiVersion: apps/v1
kind: Deployment
metadata: {name: untouched-gf}
spec:
  replicas: 1
  selector: {matchLabels: {app: untouched-gf}}
  template:
    metadata: {labels: {app: untouched-gf}}
    spec:
      containers: [{name: c, image: nginx:alpine}]
