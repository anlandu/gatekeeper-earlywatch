apiVersion: apps/v1
kind: Deployment
metadata: {name: untouched}
spec:
  replicas: 1
  selector: {matchLabels: {app: untouched}}
  template:
    metadata: {labels: {app: untouched}}
    spec:
      containers: [{name: c, image: nginx:alpine}]
