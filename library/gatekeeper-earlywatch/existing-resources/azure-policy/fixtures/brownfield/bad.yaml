apiVersion: v1
kind: Service
metadata: {name: bf-svc-bad}
spec:
  selector: {app: bf-bad}
  ports: [{port: 80}]
---
apiVersion: v1
kind: Pod
metadata: {name: bf-pod, labels: {app: bf-bad}}
spec:
  restartPolicy: Never
  containers: [{name: c, image: nginx:alpine}]
