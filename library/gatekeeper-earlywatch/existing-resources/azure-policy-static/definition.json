{
  "properties": {
    "displayName": "EarlyWatch: ExistingResources (static selector) - block Service delete with production Pods",
    "policyType": "Custom",
    "mode": "Microsoft.Kubernetes.Data",
    "description": "Denies operations on a Service when a Pod with a configurable label key/value still exists in the namespace.",
    "metadata": { "category": "Kubernetes" },
    "parameters": {
      "effect": { "type": "String", "allowedValues": ["audit", "deny", "disabled"], "defaultValue": "deny" },
      "excludedNamespaces": { "type": "Array", "defaultValue": ["kube-system", "gatekeeper-system", "azure-arc", "azure-extensions-usage-system"] },
      "namespaces": { "type": "Array", "defaultValue": [] },
      "labelSelector": { "type": "Object", "defaultValue": {} },
      "operations": { "type": "Array", "metadata": { "displayName": "Operations to deny" }, "defaultValue": ["DELETE"] },
      "labelKey": { "type": "String", "metadata": { "displayName": "Pod label key to inspect" }, "defaultValue": "tier" },
      "labelValues": { "type": "Array", "metadata": { "displayName": "Pod label values that trigger deny" }, "defaultValue": ["production", "staging"] }
    },
    "policyRule": {
      "if": {
        "field": "type",
        "in": [
          "Microsoft.ContainerService/managedClusters",
          "Microsoft.Kubernetes/connectedClusters",
          "AKS Engine"
        ]
      },
      "then": {
        "effect": "[parameters('effect')]",
        "details": {
          "templateInfo": { "sourceType": "PublicURL", "url": "https://raw.githubusercontent.com/anlandu/gatekeeper-earlywatch/refs/heads/main/library/gatekeeper-earlywatch/existing-resources/template-static.yaml" },
          "apiGroups": [""],
          "kinds": ["Service"],
          "namespaces": "[parameters('namespaces')]",
          "excludedNamespaces": "[parameters('excludedNamespaces')]",
          "labelSelector": "[parameters('labelSelector')]",
          "values": {
            "operations": "[parameters('operations')]",
            "labelKey": "[parameters('labelKey')]",
            "labelValues": "[parameters('labelValues')]"
          }
        }
      }
    }
  }
}
