apiVersion: v1
kind: Service
metadata: {name: svc-good}
spec:
  selector: {app: x}
  ports: [{port: 80}]
---
apiVersion: v1
kind: Pod
metadata: {name: dev-pod, labels: {tier: dev}}
spec:
  restartPolicy: Never
  containers: [{name: c, image: nginx:alpine}]
