apiVersion: constraints.gatekeeper.sh/v1beta1
kind: EWNameReferenceCheck
metadata:
  name: configmap-not-referenced
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["ConfigMap"]
  parameters:
    sameNamespace: true
    operations: ["DELETE"]
